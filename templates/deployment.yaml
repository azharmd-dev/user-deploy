apiVersion: apps/v1
kind: Deployment 
metadata:
  name: user
  namespace: robomart
  labels: 
    project: {{ .Values.deployment.project }}
    component: {{ .Values.deployment.component }}
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      project: {{ .Values.deployment.project }}
      component: {{ .Values.deployment.component }}
      tier: backend
  template:
    metadata:
      labels:
        project: {{ .Values.deployment.project }}
        component: {{ .Values.deployment.component }}
        tier: backend
    spec:
      containers:
      - name: user
        image: "{{ .Values.deployment.imageRepo }}/{{ .Values.deployment.component }}:{{ .Values.deployment.imageVersion }}"
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: user
        ports:
        - name: liveness-port
          containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: liveness-port
          failureThreshold: 1
          periodSeconds: 20
        livenessProbe:
          httpGet:
            path: /health
            port: liveness-port
          failureThreshold: 1
          periodSeconds: 30
